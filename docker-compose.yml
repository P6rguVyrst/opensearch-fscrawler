---
# FSCrawler Python edition — docker-compose development stack
# Services: OpenSearch, OpenSearch Dashboards, Apache Tika, FSCrawler
#
# Usage:
#   docker compose up -d            # start everything
#   docker compose logs -f fscrawler
#   docker compose down             # stop and remove containers
#
# Network security:
#   All host port mappings bind to 127.0.0.1 so services are reachable from
#   your laptop (browser, curl, etc.) but not from the wider network.
#
#   The config/_settings.yaml files use rest.url: "http://0.0.0.0:8080" because
#   that address is evaluated *inside the container* — 0.0.0.0 means "accept
#   connections on any container interface", which is required for the host port
#   mapping to reach the process.  The 127.0.0.1 prefix on the *host side*
#   (e.g. "127.0.0.1:8080:8080") is what limits external exposure.
#
#   In short:  container binds 0.0.0.0 (required) → docker-compose exposes
#   via 127.0.0.1 (safe).  Do NOT remove the 127.0.0.1 prefix unless you
#   understand the security implications (see SECURITY.md).

services:

  opensearch:
    image: opensearchproject/opensearch:3.5.0@sha256:dbb01641baadae5104e18acd888bf05e8fdd9af3567fd30624a76ba3e5a31dec
    container_name: fscrawler-opensearch
    environment:
      - cluster.name=opensearch-cluster
      - node.name=opensearch-node1
      - discovery.type=single-node
      - OPENSEARCH_INITIAL_ADMIN_PASSWORD=${OPENSEARCH_INITIAL_ADMIN_PASSWORD} # required ≥ 2.12
      - plugins.security.disabled=true                                         # disable security for dev
      - bootstrap.memory_lock=true
      - "OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m"
    ulimits:
      memlock:
        soft: -1
        hard: -1
    ports:
      - "127.0.0.1:9200:9200"
      - "127.0.0.1:9600:9600"
    volumes:
      - opensearch-data:/usr/share/opensearch/data
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:9200/_cluster/health | grep -q '\"status\":\"green\\|yellow\"'"]
      interval: 15s
      timeout: 10s
      retries: 10

  opensearch-dashboards:
    image: opensearchproject/opensearch-dashboards:3.5.0@sha256:03afc96f2f50a02246294aaa37adffc41270104676eff7c5f377c40ae9dd1ff2
    container_name: fscrawler-dashboards
    depends_on:
      opensearch:
        condition: service_healthy
    environment:
      - OPENSEARCH_HOSTS=["http://opensearch:9200"]
      - DISABLE_SECURITY_DASHBOARDS_PLUGIN=true
    ports:
      - "127.0.0.1:5601:5601"

  tika:
    image: apache/tika:latest-full@sha256:ecdd37e204308266b1f8e28569f8516ebd5b96d7a4518a71d3904710b20c03b3
    container_name: fscrawler-tika
    ports:
      - "127.0.0.1:9998:9998"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9998/tika | grep -q 'Apache Tika'"]
      interval: 10s
      timeout: 5s
      retries: 10

  fscrawler-markdown:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fscrawler-markdown
    depends_on:
      opensearch:
        condition: service_healthy
      tika:
        condition: service_healthy
    volumes:
      - ./config:/home/fscrawler/.fscrawler
      - ./playground/data:/data
    ports:
      - "127.0.0.1:8080:8080"
    command: ["--config_dir", "/home/fscrawler/.fscrawler", "--loop", "--", "markdown"]

  fscrawler-pdf:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fscrawler-pdf
    depends_on:
      opensearch:
        condition: service_healthy
      tika:
        condition: service_healthy
    volumes:
      - ./config:/home/fscrawler/.fscrawler
      - ./playground/data:/data
    ports:
      - "127.0.0.1:8081:8081"
    command: ["--config_dir", "/home/fscrawler/.fscrawler", "--loop", "--", "pdf"]

  fscrawler-catchall:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fscrawler-catchall
    depends_on:
      opensearch:
        condition: service_healthy
      tika:
        condition: service_healthy
    volumes:
      - ./config:/home/fscrawler/.fscrawler
      - ./playground/data:/data
    ports:
      - "127.0.0.1:8082:8082"
    command: ["--config_dir", "/home/fscrawler/.fscrawler", "--loop", "--", "catchall"]

volumes:
  opensearch-data:
    driver: local
